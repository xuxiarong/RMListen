<resources>

    <style name="Theme.RMListen" parent="Theme.MaterialComponents.Light" />

    <style name="Theme.RMListen.Fullscreen" parent="Theme.RMListen">
        <item name="android:actionBarStyle">@style/Widget.Theme.RMListen.ActionBar.Fullscreen</item>
        <item name="android:windowActionBarOverlay">true</item>
        <item name="android:windowBackground">@null</item>
    </style>

    <style name="ThemeOverlay.RMListen.FullscreenContainer" parent="">
        <item name="fullscreenBackgroundColor">@color/light_blue_600</item>
        <item name="fullscreenTextColor">@color/light_blue_A200</item>
    </style>
</resources>