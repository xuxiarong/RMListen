<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.rm.module_mine">

    <application
        android:name="debug.MineApplication"
        android:icon="@mipmap/mine_ic_launcher"
        android:label="@string/mine_app_name"
        android:supportsRtl="true"
        android:theme="@style/BusinessAppTheme">
        <activity android:name="debug.MineMainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>